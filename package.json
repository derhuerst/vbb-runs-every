{
	"name":         "vbb-runs-every",
	"description":  "Compute frequencies of departures and routes.",
	"version":      "0.1.0",
	"main":         "index.js",
	"files":        ["index.js", "data/*.ndjson.gz"],
	"keywords":     ["vbb", "schedule", "departures", "routes", "minutes"],
	"author":       "Jannis R <mail@jannisr.de>",
	"homepage":     "https://github.com/derhuerst/vbb-runs-every",
	"repository":   "git://github.com/derhuerst/vbb-runs-every.git",
	"license":      "ISC",
	"engines":      {"node": ">=6"},
	"dependencies": {
		"ioredis": "^2.2"
	},
	"devDependencies": {
		"multipipe": "^1.0.2",
		"vbb-trips": "^1.1.1",
		"through2": "^2.0.1",
		"ndjson": "^1.5.0",
		"slice-file": "^1.0.0",
		"queue": "^4.0.0"
	},
	"scripts": {
		"cleanup": "rm -f build/*.txt.gz",
		"convert": "node build/convert.js",
		"combine": "cat build/*.txt.gz | gunzip | gzip -9 > build/all.txt.gz",
		"prepublish": "npm run cleanup && npm run convert && npm run combine"
	}
}
